# Dependências para testes avulsos em tests/python
# Uso: pip install -r tests/python/requirements.txt
# Pinados p/ espelhar a imagem de prod (senão testes rodam contra SDK diferente).
anthropic==1.3.0
python-dotenv==1.2.3
