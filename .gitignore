# Dependencies
node_modules/
node_modules
__pycache__/
*.py[cod]
.Python
venv/
.venv/
env/
.env
.env.local
.env.*.local
# Env extra de prod do Deadpool (chaves AWS do tenant + CREDENTIALS_ENCRYPTION_KEY do fork B).
# Vive só no host de prod (root:600); NUNCA deve ser commitado. Blinda contra git add -A acidental.
.env.deadpool-aws

# Build outputs
dist/
build/
*.egg-info/
.eggs/
.next/

# Cruft — nunca versionar (o repo usa npm/package-lock.json)
pnpm-lock.yaml
._*
.AppleDouble

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Test / Coverage
# Resumos E2E com carimbo de data (gerados localmente; não versionar)
tests/e2e/reports/summary_[0-9]*.txt
coverage/
.nyc_output/
.pytest_cache/
.coverage
htmlcov/

# Secrets
*.pem
*.key
secrets/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# Multipart upload artifacts (product ZIP ingest, spec attachments) — runtime only
applications/services/api-node/uploads/

# Bedrock creds override (credenciais temporárias em claro — nunca commitar)
docker-compose.override.bedrock.yml

# Runner state local (checkpoints de execução — runtime)
applications/orchestrator/state/

docker-compose.override.foundry.yml
