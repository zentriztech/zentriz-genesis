# Usado pelo build do serviço agents (contexto = raiz do repo).
# Incluir apenas: applications/agents, applications/contracts, applications/orchestrator

# Projeto (não faz parte da imagem do produto)
project/

# Outras aplicações (api e genesis-web têm build próprio)
applications/apps/
applications/services/

# Controle de versão e cache
.git
.gitignore
node_modules
.next

# Ambiente e arquivos locais
.env
.env.*
*.log
.DS_Store

# Scripts e config da raiz (não precisam no container)
docker-compose.yml
docker-compose*.yml
deploy-docker.sh
*.md
