# Usado pelo build do agents-backend (contexto = raiz do repo).
# Incluir apenas: agents/, contracts/, docs/, orchestrator/

# Apps e serviços (não usados pelo agents-backend)
apps/
services/

# Outros diretórios desnecessários para o build
context/
spec/
infra/
k8s/
tests/

# Controle de versão e cache
.git
.gitignore
node_modules
.next

# Ambiente e arquivos locais
.env
.env.*
*.log
.DS_Store

# Scripts e config da raiz (não precisam no container)
docker-compose.yml
docker-compose*.yml
deploy-docker.sh
*.md
